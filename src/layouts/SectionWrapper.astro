---
const { class: className = "", id = "" } = Astro.props;
---

<section
  class={`w-full flex items-center justify-center ${className}`}
  id={id}
>
  <div class="w-full max-w-screen-2xl px-6 md:px-12 py-6 md:py-12">
    <slot />
  </div>
</section>
