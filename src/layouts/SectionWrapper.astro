---
const { class: className } = Astro.props;
---

<section
  class={`w-full md:h-[calc(100vh-64px)] md:max-h-[790px] flex items-center justify-center ${className}`}
>
  <div class="w-full max-w-[1440px] px-6 md:px-20 py-20">
    <slot />
  </div>
</section>
