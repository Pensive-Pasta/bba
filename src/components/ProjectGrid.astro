---
import type { Project } from "../types/Project";
import ProjectCard from "./ProjectCard.astro";
const { projects } = Astro.props as { projects: Project[] };
---

<div
  class="project-grid grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 transition-opacity duration-300 ease-in-out opacity-100 max-w-[1600px] mx-auto"
>
  {
    projects.map((project) => (
      <div class="card" data-type={project.type}>
        <ProjectCard {...project} />
      </div>
    ))
  }
</div>
